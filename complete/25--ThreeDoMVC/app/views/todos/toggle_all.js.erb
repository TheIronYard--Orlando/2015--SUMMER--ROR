$("#todo-list li").<%= @toggle_to ? 'add' : 'remove' %>Class('completed').find('input[type="checkbox"]').prop('checked', <%= @toggle_to %>);

<%= render partial: 'bottom_row', locals: { count: @count,
                                            complete: @any_complete } %>